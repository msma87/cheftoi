<h1> Seguem os detalhes dos servicos que voce oferece</h1>
      <% @services.each do |service|%>
          <%= service.title%>
          <%= service.category%>
          R$ <%= number_with_precision(service.price, precision: 2) %>
          <%= service.user.id%>
      <%end%>
