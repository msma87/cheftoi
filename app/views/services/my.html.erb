<h1> Seguem os detalhes dos serviços desse chefe</h1>
      <% @services.each do |service| %>
          <%= service.title%>
          <%= service.category%>
          <%= service.price%>
          <%= service.user.id%>
      <% end %>
